🧠 Venaris – Daily Delta

Date: 2026-04-13
Session Goal: Camera-UX entlang der Input-Logik vereinfachen und Event/Detail-Bearbeitung konsistent auf Ingest ausrichten.

✅ Done Today

- Cameras-Untermenü bereinigt: Events als sichtbarer Menüpunkt entfernt.
- Cameras-Untermenü neu sortiert: Overview → New → Ingest → Import → Health.
- Active-State angepasst: Event-Detailseite unter /cameras/events/[id] aktiviert nun im Menü Ingest statt Events.
- Wildlife-CTAs von /cameras/events auf /cameras/ingest umgestellt.
- Ingest-Übersicht bereinigt: event_count-/Aufnahmen-Logik aus der Tabelle entschärft und Ergebnisdarstellung vereinfacht.
- Event-Detailseite UX deutlich überarbeitet: großes Hero-Bild links, Kerninfos rechts.
- Event-Detailseite um konsistente Dropdown-Steuerung für Relevant und Species ergänzt.
- Species-Override technisch eingeführt über neue API-Route /api/asset-species.
- Species-Override so verdrahtet, dass er in die bestehende Wildlife-Logik hineinwirkt.
- demoAssetResolver auf species_user / species-Override angepasst.
- Demo-Write-Schutz auf Event-Detailseite umgesetzt: Demo-User können dort nichts speichern.
- Cameras Overview bereinigt: kryptische Asset-IDs, storage_path und alte Relevant-Markieren-Buttons entfernt.
- Cameras Overview von Arbeitskontext/Quick Upload befreit.
- Cameras Overview auf kompakteren Kamerafilter umgestellt.
- Attention-Block vor den Kamerafilter gezogen.

🟡 Changed System Behavior

- Event-Detailbearbeitung erfolgt jetzt über zwei einheitliche Dropdowns: Relevant und Species.
- Species-Overrides schreiben nicht nur UI-lokal, sondern beeinflussen die nachgelagerte Wildlife-Auswertung.
- Event-Detailseiten bleiben technisch unter /cameras/events/[id], werden UX-seitig aber als Teil von Ingest behandelt.
- Demo-User können auf der Event-Detailseite weder Relevant noch Species speichern.
- Cameras Overview zeigt nur noch nutzerrelevante Asset-Infos statt technischer Dateipfade/UUIDs.

🔴 New Problems / Risks

- Die technische Route /cameras/events/[id] existiert weiterhin, obwohl der sichtbare Navigationspfad jetzt Ingest ist. Das ist bewusst minimalinvasiv, bleibt aber ein späterer Aufräumpunkt.
- Die alte Seite /cameras/events/page.tsx existiert weiterhin technisch und sollte später entschieden oder entfernt werden.
- Wildlife und Home hängen an Species-/Event-Logik; künftige Änderungen an species_user müssen weiter systemweit konsistent gedacht werden.
- Cameras Overview lädt Asset-Preview-URLs weiterhin clientseitig pro Asset; kein neues Problem, aber weiter ein potenzieller Performance-Punkt.

🧩 Current Focus

- Camera/Input-UX weiter vereinfachen und konsistent halten.
- Event-/Ingest-Flow stabilisieren, ohne große Route-Refactors am Live-System.

📍 Next Immediate Step

- Nächster sinnvoller Block ist Health-UX wieder aufnehmen, insbesondere Konfig-/Zugangsdaten-Frage sauber und UX-tauglich lösen.

🧠 Context I Must Not Forget

- Wildlife ist Output, Cameras ist Input. Keine Wildlife-Navigation in Cameras einziehen.
- Event-Detail bleibt vorerst technisch unter /cameras/events/[id], wird aber UX-seitig Ingest zugeordnet.
- species_user existiert und ist jetzt Teil der wirksamen Species-Logik.
- asset_species_summary nutzt COALESCE(species_user, species).
- get_activity_by_hour wurde bereits auf species_user/override-Logik angepasst.
- Demo-Accounts sind read-only; Event-Detail-Controls respektieren das jetzt.
- Schritt-für-Schritt am offenen Herzen: minimalinvasiv arbeiten, keine Big-Bang-Refactors.