🧠 Venaris – Daily Delta

Date: 2026-03-26
Session Goal: Camera-Health neu als saubere Management-Übersicht aufsetzen und den Revierfilter auf die relevanten Camera-Seiten/-APIs ausrollen.

✅ Done Today

- /cameras/health von einem clientseitigen Mixed-Screen zu einer serverseitigen Management-Übersicht umgebaut.
- Camera Health zeigt jetzt eine tabellarische Übersicht mit Kamera, Revier, Methode, Health, Status, Last Feed und Aktionen.
- Camera Health unterstützt jetzt Active/Disabled pro Kamera über cameras.is_active.
- Camera Health unterstützt jetzt echten Kamera-Delete per Mülleimer; abhängige Daten werden über bestehende FK-Cascades mitgelöscht.
- Save-Action auf Camera Health ist jetzt nur aktiv, wenn sich der Status tatsächlich geändert hat.
- Camera Health respektiert jetzt den globalen Revierfilter serverseitig analog zu den Wildlife-Seiten.
- Revierfilter-Unterstützung in den zugehörigen Camera-APIs ergänzt: camera-health, assets, ingest-batches, manual-cameras.
- Revierfilter auf weiteren Camera-Seiten umgesetzt: /cameras, /cameras/import, /cameras/events, /cameras/events/[id], /cameras/ingest.
- Kamera-Slot-Zählung für Subscription/Create auf aktive Kameras umgestellt; disabled Kameras zählen damit nicht mehr gegen max_cameras.
- Provisioning Result auf /cameras/new UX-seitig verbessert: statt vieler Einzel-Copy-Buttons jetzt Copy pro Ergebnisblock.

🟡 Changed System Behavior

- Camera Health ist jetzt die führende serverseitige Übersicht statt eines clientseitigen Inspector-/Operations-Mixscreens.
- cameras.is_active ist jetzt sichtbar und direkt über die UI verwaltbar.
- Re-Enable einer disabled Kamera prüft das Subscription-Limit gegen aktive Kameras.
- Neue Kameras werden bei Subscription-Prüfung nur noch gegen aktive Kameras gezählt.
- Mehrere Camera-Seiten respektieren jetzt den Header-Revierfilter statt nur org-weit zu laden.
- Import zeigt manuelle Kameras jetzt revier-scope-konform an.
- Event-Liste und Event-Detail sind jetzt revier-scope-konform.

🔴 New Problems / Risks

- camera_ingest_configs.is_active bzw. operative Ingest-Wirkung von disabled Kameras ist noch nicht fachlich/technisch durchdekliniert.
- Es wurde bewusst nur cameras.is_active für den UI-/Subscription-Use-Case aktiviert; mögliche Worker-/Provisioning-Folgen müssen separat geprüft werden.
- CameraRowControls ist durch den Split in CameraRowFields + CameraRowActions obsolet und sollte nicht versehentlich weiterverwendet werden.

🧩 Current Focus

- Revierfilter-Konsistenz auf allen verbleibenden Camera-/kamera-nahen Flows absichern.
- Disabled-Kamera-Verhalten für Ingest/Provisioning fachlich sauber festziehen.

📍 Next Immediate Step

- Prüfen, ob disabled Kameras weiterhin ingestieren dürfen oder ingest-/worker-seitig ausgeschlossen werden müssen.

🧠 Context I Must Not Forget

- Immer evidenzbasiert und step-by-step arbeiten; keine UX-Änderungen ohne Abstimmung.
- Code-Dateien müssen in Zeile 1 mit Pfad + Index kommentiert werden.
- Camera Health verwendet jetzt getrennt:
  - Health = camera_health.health_status
  - Status = cameras.is_active
- Disabled Kameras zählen nicht gegen max_cameras.
- Re-Enable braucht denselben Slot-Check wie Create.
- Kamera-Delete bleibt echter Delete in Supabase.
- Andere Camera-Seiten mussten/konnten den Revierfilter nicht implizit, sondern nur über die jeweiligen APIs bzw. serverseitige Scope-Logik bekommen.
- Später weitere Camera-Pages erneut auf Revierfilter-Konsistenz auditieren.